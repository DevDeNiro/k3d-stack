apiVersion: v1
kind: Service
metadata:
    name: kafka-nodeport
    namespace: messaging
spec:
    type: NodePort
    ports:
        -   port: 9092
            targetPort: 9092
            nodePort: 30092
            name: kafka-client
    selector:
        app.kubernetes.io/name: kafka
        app.kubernetes.io/instance: kafka